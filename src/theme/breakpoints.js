const tablet = '768px';
const desktop = '992px';
const lgDesktop = '1200px';

export const breakpoints = [tablet, desktop, lgDesktop];

// aliases
breakpoints.tablet = breakpoints[0];
breakpoints.desktop = breakpoints[1];
breakpoints.lgDesktop = breakpoints[2];
